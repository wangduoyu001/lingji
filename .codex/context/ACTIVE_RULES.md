# Active Rules

1. Never overwrite or write runtime state into the original LingJi project.
2. AI chat records are the only automatic memory source.
3. Obsidian content is manual formal knowledge; index it, but never auto-distill it.
4. Important memories remain `pending` until explicit approval.
5. Watch only configured roots; never scan an entire drive.
6. Qdrant is rebuildable. SQLite and raw archives are the sources of truth.
7. Keep all new writable state on D:.
