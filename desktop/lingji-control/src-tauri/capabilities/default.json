{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the LingJi local control window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
